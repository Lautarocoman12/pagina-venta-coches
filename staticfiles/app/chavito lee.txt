asi estaba este style.css

/* === NAVBAR OSCURO PROFESIONAL === */
.navbar-dark {
    background-color: #0d0d0d;
    padding: 15px 25px;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    box-shadow: 0 2px 6px rgba(0,0,0,0.4);
}

.nav-container {
    max-width: 1300px;
    margin: auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

/* Logo */
.nav-left .logo {
    height: 40px;
    filter: brightness(1.9);
}

/* Barra de búsqueda */
.nav-center {
    flex: 1;
    display: flex;
    justify-content: center;
    padding: 0 20px;
}

.navbar-dark .search-bar {
    width: 100%;
    max-width: 500px;
    padding: 10px 15px;
    border-radius: 8px;
    border: none;
    background: #1f1f1f;
    color: white;
    font-size: 15px;
}

.navbar-dark .search-bar::placeholder {
    color: #bbb;
}

/* Enlaces del lado derecho */
.nav-right {
    display: flex;
    align-items: center;
    gap: 20px;
}

.nav-right a {
    color: #e6e6e6;
    text-decoration: none;
    font-weight: 600;
    transition: 0.3s;
}

.nav-right a:hover {
    color: white;
}

/* Botón estilo "Crear cuenta" */
.btn-register {
    padding: 8px 14px;
    background: #1a73e8;
    color: white !important;
    border-radius: 6px;
    transition: background 0.3s ease;
}

.btn-register:hover {
    background: #135dc2;
}

/* Icono carrito */
.cart-icon {
    font-size: 21px;
    color: white;
}


tengo la teoria de q es mi css viejo y q no lo borre y q por eso tenia el error que te dije del css, lo estoy viendo recien a las 12 de la noche, lo deje vacio por las dudas, nomas te aviso xd